splunk-operator:
  enabled: true
  splunkOperator:
    clusterWideAccess: false
  operator:
    serviceAccountName: helm-service-account

    volumes:
    - name: app-staging
      emptyDir:
        sizeLimit: 10G

    volumeMounts:
    - name: app-staging
      mountPath: /opt/splunk/appframework/

standalone:
  enabled: true