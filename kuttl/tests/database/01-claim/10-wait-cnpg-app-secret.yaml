apiVersion: kuttl.dev/v1beta1
kind: TestStep
# Wait until CNPG has created the <cluster>-app secret
assert:
- apiVersion: v1
  kind: Secret
  metadata:
    name: shc-db-app
    namespace: splunk
timeout: 600    # up to 10 minutes to let storage & bootstrap happen
