---
apiVersion: kuttl.dev/v1beta1
kind: TestStep
commands:
  - command: helm install splunk-m4 $HELM_REPO_PATH/splunk-enterprise -f m4_config.yaml --set splunk-operator.splunkOperator.splunkGeneralTerms=${SPLUNK_GENERAL_TERMS_FLAG}
    namespaced: true
