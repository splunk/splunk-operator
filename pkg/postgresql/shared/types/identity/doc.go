// Package identity defines the shared PostgreSQL resource-identity contracts.
//
// Cluster and database reconciliation use these value types to agree on the
// logical PostgresCluster, its authoritative provider environment, and any
// additional environments that remain relevant to lifecycle work.
package identity
