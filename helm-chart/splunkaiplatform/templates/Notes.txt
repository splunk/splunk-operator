# templates/NOTES.txt
1. Your SplunkAIPlatform custom resource has been created:
   kubectl get splunkaiplatform {{ include "splunkaiplatform.fullname" . }}

2. If you enabled Ingress:
   * Host: {{ (index .Values.ingress.hosts 0).host }}
   * Port: 80 / 443
   * Try: curl http://{{ (index .Values.ingress.hosts 0).host }}
