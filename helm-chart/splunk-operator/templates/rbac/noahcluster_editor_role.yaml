{{- if .Values.splunkOperator.clusterWideAccess }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "splunk-operator.operator.fullname" . }}-noahcluster-editor-role
rules:
- apiGroups:
  - enterprise.splunk.com
  resources:
  - noahclusters
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
{{- else }}
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "splunk-operator.operator.fullname" . }}-noahcluster-editor-role
rules:
- apiGroups:
  - enterprise.splunk.com
  resources:
  - noahclusters
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
{{- end }}