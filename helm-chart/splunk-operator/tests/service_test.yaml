suite: controller-manager Service placement
templates:
  - templates/service.yaml
tests:
  - it: should place the Service in the effective namespace
    release:
      namespace: release-namespace
    set:
      namespaceOverride: watched-namespace
    asserts:
      - isKind:
          of: Service
      - equal:
          path: metadata.namespace
          value: watched-namespace
