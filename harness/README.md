# Harness Artifacts

This directory contains machine-readable manifests used by governance checks.

- `manifests/`: per-change harness manifests consumed by
  `scripts/dev/harness_manifest_check.sh`.

Runtime artifacts are written to `.harness/runs/` and are intentionally ignored
in git.
