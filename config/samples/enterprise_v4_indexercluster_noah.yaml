apiVersion: enterprise.splunk.com/v4
kind: IndexerCluster
metadata:
  name: indexercluster-noah-sample
  namespace: splunk-operator
  finalizers:
    - enterprise.splunk.com/delete-pvc
spec:
  replicas: 3
  noahClusterRef:
    name: noahcluster-sample
