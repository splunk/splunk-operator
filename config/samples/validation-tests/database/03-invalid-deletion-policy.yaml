apiVersion: enterprise.splunk.com/v4
kind: Database
metadata:
  name: test-invalid-policy
  namespace: default
spec:
  clusterRef:
    name: postgres-cluster
  databases:
    - name: kvstore
      deletionPolicy: Archive  # INVALID! Only "Delete" or "Retain" allowed
