---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: appruntimes.enterprise.splunk.com
spec:
  group: enterprise.splunk.com
  names:
    kind: AppRuntime
    listKind: AppRuntimeList
    plural: appruntimes
    shortNames:
    - ar
    singular: appruntime
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.message
      name: Message
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v4
    schema:
      openAPIV3Schema:
        description: AppRuntime is the Schema for a App Runtime component
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              image:
                type: string
              replicas:
                format: int32
                type: integer
              splunkImage:
                type: string
            type: object
          status:
            properties:
              message:
                description: Auxiliary message describing CR status
                type: string
              phase:
                description: current phase of the App Runtime pod
                enum:
                - Pending
                - Ready
                - Updating
                - ScalingUp
                - ScalingDown
                - Terminating
                - Error
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
