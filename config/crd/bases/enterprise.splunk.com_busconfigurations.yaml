---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: busconfigurations.enterprise.splunk.com
spec:
  group: enterprise.splunk.com
  names:
    kind: BusConfiguration
    listKind: BusConfigurationList
    plural: busconfigurations
    shortNames:
    - bus
    singular: busconfiguration
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Status of bus configuration
      jsonPath: .status.phase
      name: Phase
      type: string
    - description: Age of bus configuration resource
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - description: Auxillary message describing CR status
      jsonPath: .status.message
      name: Message
      type: string
    name: v4
    schema:
      openAPIV3Schema:
        description: BusConfiguration is the Schema for the busconfigurations API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BusConfigurationSpec defines the desired state of BusConfiguration
            properties:
              sqs:
                properties:
                  authRegion:
                    type: string
                  deadLetterQueueName:
                    type: string
                  endpoint:
                    type: string
                  largeMessageStoreEndpoint:
                    type: string
                  largeMessageStorePath:
                    type: string
                  queueName:
                    type: string
                type: object
              type:
                type: string
            type: object
          status:
            description: BusConfigurationStatus defines the observed state of BusConfiguration.
            properties:
              message:
                description: Auxillary message describing CR status
                type: string
              phase:
                description: Phase of the bus configuration
                enum:
                - Pending
                - Ready
                - Updating
                - ScalingUp
                - ScalingDown
                - Terminating
                - Error
                type: string
              resourceRevMap:
                additionalProperties:
                  type: string
                description: Resource revision tracker
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      scale:
        labelSelectorPath: .status.selector
        specReplicasPath: .spec.replicas
        statusReplicasPath: .status.replicas
      status: {}
